// src/setupTests.js

// Import @testing-library/jest-dom to extend Jest's `expect` with custom matchers
import '@testing-library/jest-dom';

// You can also add other setup code here if needed, for example, global mocks, configurations, or polyfills.
