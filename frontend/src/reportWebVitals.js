// src/reportWebVitals.js

import { reportWebVitals } from 'web-vitals';

// Custom function to log the web vitals to the console or send them somewhere
const reportWebVitals = (metric) => {
    console.log(metric); // You can send this data to an analytics service if needed
};

export default reportWebVitals;
