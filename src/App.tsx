import React from "react";
import SearchFilterApp from "./SearchFilterApp";

const App: React.FC = () => {
    return (
        <div>
            <SearchFilterApp />
        </div>
    );
};

export default App;
